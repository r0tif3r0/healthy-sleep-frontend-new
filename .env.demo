# Демо для показа заказчику: запросы к API перехватывает service worker,
# бэкенд не нужен. Адрес API любой — он служит только префиксом путей.
VITE_DEMO=1
VITE_API_BASE_URL=https://demo.api.local
VITE_PUBLIC_URL=
